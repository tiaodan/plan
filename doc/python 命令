1. 更新pip
python -m pip install --upgrade pip
2. 安装虚拟环境
pip install virtualenv
3. 创建虚拟环境
virtualenv -p python3 venv
4. 激活虚拟环境
cd venv # cd 虚拟环境目录
source venv/bin/activate   或者
. venv/bin/activate


5. 卸载安装包
pip uninstall lxml(包名)

6. 安装
pip install pymysql(包名)
